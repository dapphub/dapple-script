/* global it, describe */
'use strict';

var assert = require('chai').assert;

describe('Expert', function () {

  var classes = {
    A: {
      bytecode: "12345",
      interface: "[]"
    }
  };
  before(() => {});
  
});
